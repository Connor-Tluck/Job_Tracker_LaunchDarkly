aliases:
  # FLAG_KEYS constants use UPPER_SNAKE_CASE (e.g., show-admin-page → SHOW_ADMIN_PAGE)
  - type: uppersnakecase
  # useFeatureFlag calls sometimes store results in camelCase variables
  - type: camelcase
