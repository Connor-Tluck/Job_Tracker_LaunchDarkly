# Supabase Configuration
# Get these values from your Supabase project settings
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Authentication
# Set to "true" to enable Google OAuth authentication
# Set to "false" or leave empty to run in frontend-only mode
NEXT_PUBLIC_ENABLE_AUTH=false

# Optional: Supabase Service Role Key (only use in server-side code, never expose to client)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Optional: Google OAuth (if using Google authentication)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

